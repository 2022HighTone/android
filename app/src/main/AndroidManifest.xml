<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iamgonna.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".object.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IamGonna"
        android:usesCleartextTraffic="true"
        >
        <activity
            android:name=".login.view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".storeDetail.StoreDetailActivity"/>
        <activity android:name=".setSchool.view.SetSchoolActivity"/>
        <activity android:name=".signUp.view.SignUpActivity"/>
        <activity android:name=".main.view.MainActivity"/>
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="1310y3ukwm" />
    </application>

</manifest>